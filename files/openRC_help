#Проверка сервера постгрес  
systemctl status postgresql

# Проверка отказавших служб.
rc-status --crashed

# Проверка служб.
rc-status

# Отображение всех доступных служб.
rc-update -v showe

# Изменить состояние службы.
rc-service "myservice" (start, stop, restart, status)

# Включить или выключить службу.
rc-update (add, del) "myservice"

# Включить или выключить службу(юзал это)
rc-update add "myservice" default

 df -h

pkill -f /root/parma_bot/parma59_bot/.venv/bin/python3

'Asia/Yekaterinburg'
tmux new -s telegram_bot_session

tmux ls
tmux list-sessions
C-b w
rm -rf ~/.poetry

#detach
tmux ctrl + b --> d

tmux attach -t telegram_bot_session

Create a new window within a tmux session: Press Ctrl+b, then c.

Switch between tmux windows: Press Ctrl+b, then the window number.

Split the window vertically: Press Ctrl+b, then %.

Split the window horizontally: Press Ctrl+b, then "

Close a tmux window or pane: To close the current pane, press Ctrl+d.

Exit a tmux session: To close the entire tmux session, exit all terminal sessions running within it, and then run the exit command in the last remaining terminal session.
